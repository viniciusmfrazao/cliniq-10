/**
 * Tipos centralizados do sistema
 * 
 * Uso:
 * import { User, Patient, Appointment } from '@/types'
 * import { USER_ROLES, APPOINTMENT_STATUS } from '@/types'
 */

export * from './database'
